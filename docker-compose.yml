services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: magnossao
      POSTGRES_USER: magnossao
      POSTGRES_PASSWORD: magnossao
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
